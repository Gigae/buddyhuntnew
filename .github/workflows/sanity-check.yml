name: Sanity Check

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Actions laufen ✅"
